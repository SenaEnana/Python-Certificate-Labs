# Questions

# 1 What is polymorphism in object-oriented programming?

# When a class cannot have methods with the same name.

# When different classes can use the same method name but implement it differently.//correct

# When a method must be unique across all classes.

# When a child class deletes a parent class's method.

# 2 In inheritance-based polymorphism, what must a child class do to provide its own implementation of a method defined in the parent class?

# Override the method by redefining it with the same name.////correct

# Declare the method as @static.

# Delete the parent method first.

# Use the @polymorphic decorator.

#3 What is the key benefit of inheritance-based polymorphism?

# It forces all child classes to implement identical behavior.

# It prevents child classes from modifying parent methods.

# It allows calling the same method name on different child objects, with each executing its own version.////correct

# It merges all child methods into the parent class.